const superheros: string[] = []
const heroPower: Array<number> = []

type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = []

superheros.push("nik")
heroPower.push(1)

allUsers.push({name: 'nik', isActive: true}) 

export{}